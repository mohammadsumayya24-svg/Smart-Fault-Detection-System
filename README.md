# Smart-Fault-Detection-System
An IoT-based smart fault detection system (simulation) that uses virtual sensor data to detect failures in individual components of appliances and vehicles, enabling targeted repair instead of replacing the entire system.
